VScode Extensions to get
Auto close tag
auto rename tag
code spell checker
IntelliCode
npm Intellisense

Maybe needed:
ESLint

admincheck not running on createuser


Cleared:
- USER MANAGEMENT  TABLE (OK)
- Conditional rendering for admin priviledges (manage users button in header), remove buttons before login (OK)

import { isAlphaNumeric, charLengthCompliant etc } from "../utils/utils.js"
const { username, password } = req.body
  const meetsConstraints = isAlphaNumeric(username) && charLengthCompliant(username, 3, 20) //check for username compliance

  if (!meetsConstraints{
    return res.status(401).json({
      success: false,
      err: "username non compliant"
    })
  }

e.target.reset not working in createuserform

frontend
- state for login 
- protect routes

- Implement check on state chage(or re-render?) for jwt, do logout if no valid jwt //done?
- Implement groups/permissions check on state change in case permissions are revoked
- Implement checkGroup route for frontend to call so u can checkgroup at any time

backend
- re-implement verify groups in authController if got time